services:
  - type: web
    name: garage-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      BOT_TOKEN: 8307766905:AAFZL43MHWZ-yKxfgOsnQtxhXrt2L1DHVS8
